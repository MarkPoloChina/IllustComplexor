// import { FilenameComparator } from "./FilenameComparator";

// var fs = window.require("fs-extra");
// const path = 'E:\\Pictures\\BA'


/**
 * Used to start load of file system.
 * @class
 */
export class LoadBase {
  static test = () => {
    // console.log(FilenameComparator.getMatchedPixivId('96502418_p0.jpg'))
    
    // fs.readdir(path, (err, files) => {
    //   files.forEach(ele => {
    //     console.log(FilenameComparator.getBaseFilename(ele))
        
    //   })
    // })
  }
}